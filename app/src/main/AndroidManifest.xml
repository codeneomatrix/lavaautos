<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.neomatrix.lavaautos">

    <application
        android:name=".ClaseGlobal"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.myAppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".crearcita" />
        <activity android:name=".lavado" />
        <activity android:name=".hora" />
        <activity android:name=".pago" />
        <activity android:name=".fin" />
        <activity android:name=".fecha" />
        <activity android:name=".elehora" />
        <activity android:name=".carro" />
        <activity android:name=".qsomos" />
        <activity android:name=".contacto"></activity>
    </application>

</manifest>